class FriendModel {
  final String profilePic; // URL to the friend's profile picture
  final String contactName; // Friend's display name
  final String userId; // Unique ID for the friend

  FriendModel({
    required this.profilePic,
    required this.contactName,
    required this.userId,
  });
}
